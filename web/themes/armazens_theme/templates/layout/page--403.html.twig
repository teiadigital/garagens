
{#
/**
 * @file
 * Theme override to display a single page.
 *
 * @see template_preprocess_page()
 * @see armazens_theme_preprocess_page()
 */
#}

{# ── Header ───────────────────────────────── #}
<header id="site-header" role="banner" class="fixed top-4 md:top-10 left-0 w-full z-50 transition-all duration-300">
  <div class="container px-4">
    <nav class="flex items-center justify-between" aria-label="Global">
      <div class="change-logo-color">
        {{ page.header.armazens_theme_imagemdemarcadositio }}
      </div>
      <div class="flex items-center gap-2 sm:gap-3">
        {{ page.header.armazens_theme_notificacoesdereservas }}
        {{ page.header.armazens_theme_seletordeidioma }}
        {{ page.header.armazens_theme_usermenuavatardropdown }}
      </div>
    </nav>
  </div>
</header>

<script>
  (function () {
    var header = document.getElementById('site-header');
    if (!header) return;
    function onScroll() {
      if (window.scrollY > 40) {
        header.style.top = '0';
        header.style.background = '#fff';
        header.style.boxShadow = '0 1px 8px rgba(0,0,0,0.07)';
        header.style.paddingTop = '12px';
        header.style.paddingBottom = '12px';
      } else {
        header.style.top = '';
        header.style.background = '';
        header.style.boxShadow = '';
        header.style.paddingTop = '';
        header.style.paddingBottom = '';
      }
    }
    window.addEventListener('scroll', onScroll, { passive: true });
    onScroll();
  })();
</script>

{# ── Conteúdo ─────────────────────────────── #}
<main role="main" class="min-h-screen flex w-full {% if is_auth_page %} bg-no-repeat bg-right-top lg:bg-[url('/themes/armazens_theme/images/bg_small.png')]{% endif %}">
    <div class="grid grid-cols-12 flex-1 ">

        <div class="pb-16 sm:px-4 md:px-14 col-span-full pt-52 ">
            {{ page.content }}
        </div>
    </div>
</main>

{# ── Footer — o region--footer.html.twig trata do seu próprio layout #}
{% if page.footer %}
  <footer role="contentinfo">
    {{ page.footer }}
  </footer>
{% endif %}
